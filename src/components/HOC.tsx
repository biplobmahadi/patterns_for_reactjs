// When using multiple composed HOCs that all pass props to the element that's wrapped within them,
// it can be difficult to figure out which HOC is responsible for which prop.

// use hooks if it possible, tn HOC